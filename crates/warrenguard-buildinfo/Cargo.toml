# WarrenGuard engine crate: a generic VPN-over-QUIC building block with no
# Warren-specific logic, reusable by any deployer.
[package]
name = "warrenguard-buildinfo"
description = "Compile-time build metadata (git SHA + build time) baked into binaries (WarrenGuard engine)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
publish.workspace = true

[lib]
name = "warrenguard_buildinfo"
path = "src/lib.rs"

[lints]
workspace = true
