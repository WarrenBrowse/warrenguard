# WarrenGuard engine crate: a generic VPN-over-QUIC building block with no
# Warren-specific logic, reusable by any deployer.
[package]
name = "warrenguard-ratelimit"
description = "Token-bucket data-path rate limiter, keyed per identity or per tunnel (WarrenGuard engine)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
parking_lot = { workspace = true }

[lints]
workspace = true
